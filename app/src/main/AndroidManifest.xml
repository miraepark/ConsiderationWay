<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.samsung.considerationway">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="지하철노선도"
        android:supportsRtl="true"
        android:theme="@style/NoTitle">
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Call" />
        <activity android:name=".SearchListview" />
        <activity android:name=".TimetableActivity" />
        <activity android:name=".HelpdisabledActivity" />
        <activity android:name=".ArrivingInfoActivity"></activity>
    </application>

</manifest>